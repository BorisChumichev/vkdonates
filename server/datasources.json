{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "postgres": {
    "name": "postgres",
    "connector": "postgresql",
    "host": "${DB_HOST}",
    "user": "${DB_USER}",
    "password": "${DB_PASSWORD}",
    "database": "${DB_DATABASE}"
  }
}
